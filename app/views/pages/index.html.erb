<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.1.3/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Bitter:400,700">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800&family=Open+Sans:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <%= stylesheet_pack_tag 'packs/index' %>
</head>
<body>
        <div class="header-dark h-100 w-100">
            <nav class="navbar navbar-dark navbar-expand-md navigation-clean-search">
      <div class="container w-100"><a class="navbar-brand" href="/">Coda</a><button class="navbar-toggler" data-toggle="collapse" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
          <div class="collapse navbar-collapse w-75"
              id="navcol-1">

              <ul class="nav navbar-nav">

              <% if user_signed_in? %>

              <%= link_to "Add Movie", new_movie_path, class: "nav-link" %></li>

                <%= link_to "Account", edit_user_registration_path, class: "nav-link" %></li>
                 <%end%>
              </ul>


                <form class="form-inline mr-auto" action= "/search">
                  <div class="form-group"><label for="search-field"><i class="fa fa-search"></i></label><input class="form-control search-field" placeholder = "search" type="search" name="q" id="search-field"></div>
              </form>



              </form>
              <% if user_signed_in? %>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %></li>
              <% else %>
              <span class="navbar-text">
              <li><%= link_to "Login", new_user_session_path %></li></span>
              <li><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-light action-button" %></li>
              <% end %>
          </div>
        </div>
    </nav>
            <div class="container hero w-100">
                <div class="row">
                    <div class="col-md-8 offset-md-2">
                        <br>
                        <br>
                        <br>
                        <br>
                        <br>
                        <h1 class="text-center text-light bg-info"> We focus on exposure.<h1>
                    </div>
                </div>
            </div>
        </div>
    <div class="container-fluid bg-dark">
    <br>
      <h1 class="text-center text-light"> Recently added <h1>
      <br>
      <br>
      <div class="row mx-auto">
        <% Movie.all.each do |movie| %>
              <%= link_to (image_tag movie.image.url(:medium), class: ' m-3 img-thumbnail img-fluid rounded'), movie %>
          <% end %>
      </div>
   </div>
</body>
</html>